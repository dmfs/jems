import static org.junit.Assert.*;
import org.junit.Test;
import ${CLASS_NAME};


/**
 * Test for {@link ${CLASS_NAME}}.
 *
 * @author ${USER}
 */
public final class ${NAME} {

  @Test
  public void test()
  {
      assertThat(new ${CLASS_NAME}(), );
  }
  
  ${BODY}
}